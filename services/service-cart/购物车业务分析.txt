购物车技术架构：
1、不用MySQL保存购物车数据
读写都多的业务;
读多: 加缓存，都只使用数据库? 多从库
写多: 直接写库。没有自动分片机制。可以有手动分片.
      MySQL1 MySQL2 MySQL3; 手动维护



没有数据库的复杂crud。没有联表，没有模糊检索，无需group by等

购物车: 所有数据都存redis;做好redis的持久化【aof、rdb】;0(1)
1、查询购物车中所有数据
2、写购物车某个数据 (增、删、改)

2、用户不登录也能使用购物车
临时身份: 如果用户第一次使用购物车且没登录。给他发一个临时身份令牌.
        让浏览器保存[利用持久化cookie让浏览器在某段时间之前一直存着]
        浏览器以后每个请求都会带上临时id;lT
        前端很自觉，只要没登陆就会有个临时id，一年有效，以后访问都带上

     用户购物车:
        cart:info:用户id ==》 商品的hash信息
     临时购物车:
        cart:info:临时id ==》商品的hash信息